---
created: 2026-06-03T18:54:08.580Z
session: e58ef590-2db7-455c-937c-57e7ff77a0bd
working_directory: D:\terraform-devops-project
---


## CI/CD with GitHub Actions for Terraform - Complete Plan (Tanglish)

### Context

- Current repo structure: `environments/dev/` + `modules/` (vpc, security-group, ec2)
- Backend already configured with S3 (`terra-satatee` bucket)
- Module source paths are **absolute local paths** (`D:/terraform-devops-project/modules/...`) -- idha relative paths ku maathanum for CI/CD to work
- No `.gitignore` or `.github/` folder exists yet

---

## Part 1: Repository Structure Design

### Recommended Folder Structure:

```
terraform-devops-project/
├── .github/
│   └── workflows/
│       ├── terraform-dev.yml        # Dev pipeline
│       ├── terraform-staging.yml    # Staging pipeline
│       └── terraform-prod.yml       # Prod pipeline
├── environments/
│   ├── dev/
│   │   ├── backend.tf
│   │   ├── provider.tf
│   │   ├── main.tf
│   │   ├── variables.tf
│   │   ├── outputs.tf
│   │   └── terraform.tfvars
│   ├── staging/
│   │   ├── backend.tf              # Different S3 key: staging/terraform.tfstate
│   │   ├── provider.tf
│   │   ├── main.tf
│   │   ├── variables.tf
│   │   ├── outputs.tf
│   │   └── terraform.tfvars        # Staging-specific values (bigger instances, etc.)
│   └── prod/
│       ├── backend.tf              # Different S3 key: prod/terraform.tfstate
│       ├── provider.tf
│       ├── main.tf
│       ├── variables.tf
│       ├── outputs.tf
│       └── terraform.tfvars        # Prod-specific values
├── modules/
│   ├── vpc/
│   ├── security-group/
│   └── ec2/
└── .gitignore
```

---

## Part 2: Key Changes Needed

### 1. Fix Module Source Paths (CRITICAL)

Current (won't work in CI/CD):
```hcl
source = "D:/terraform-devops-project/modules/vpc"
```

Change to relative path:
```hcl
source = "../../modules/vpc"
```

### 2. Create `.gitignore`

```
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl
*.tfvars     # (optional -- some teams commit this, some don't)
```

### 3. Create `staging/` and `prod/` environments (copy from dev, change values)

### 4. Store AWS credentials as GitHub Secrets

In GitHub repo → Settings → Secrets and Variables → Actions:
- `AWS_ACCESS_KEY_ID`
- `AWS_SECRET_ACCESS_KEY`

(Or better: use OIDC with `aws-actions/configure-aws-credentials`)

---

## Part 3: Pipeline Design

### Flow:

```
Feature Branch ──► PR to main
                      │
        ┌─────────────┼─────────────────┐
        ▼             ▼                  ▼
   DEV: plan     STAGING: plan      PROD: plan
   (auto run)    (auto run)         (auto run)
        │             │                  │
        ▼             ▼                  ▼
   DEV: apply    STAGING: apply     PROD: apply
   (auto on      (manual            (manual approval
    merge)        approval)          REQUIRED)
```

### GitHub Actions Workflow Files:

#### `.github/workflows/terraform-dev.yml`

```yaml
name: "Terraform Dev"

on:
  push:
    branches: [main]
    paths: ["environments/dev/**", "modules/**"]
  pull_request:
    branches: [main]
    paths: ["environments/dev/**", "modules/**"]

env:
  TF_VERSION: "1.10.0"
  WORKING_DIR: "environments/dev"

jobs:
  terraform-plan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Terraform Format Check
        run: terraform fmt -check -recursive
        working-directory: ${{ env.WORKING_DIR }}

      - name: Terraform Init
        run: terraform init
        working-directory: ${{ env.WORKING_DIR }}

      - name: Terraform Validate
        run: terraform validate
        working-directory: ${{ env.WORKING_DIR }}

      - name: Terraform Plan
        run: terraform plan -no-color
        working-directory: ${{ env.WORKING_DIR }}

  terraform-apply:
    needs: terraform-plan
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Terraform Init
        run: terraform init
        working-directory: ${{ env.WORKING_DIR }}

      - name: Terraform Apply
        run: terraform apply -auto-approve
        working-directory: ${{ env.WORKING_DIR }}
```

#### `.github/workflows/terraform-prod.yml` (with manual approval)

```yaml
name: "Terraform Prod"

on:
  push:
    branches: [main]
    paths: ["environments/prod/**", "modules/**"]

jobs:
  terraform-plan:
    runs-on: ubuntu-latest
    steps:
      # ... same as dev (init, validate, plan)

  terraform-apply:
    needs: terraform-plan
    runs-on: ubuntu-latest
    environment: production        # ◄── This enables manual approval!
    steps:
      # ... same steps with apply
```

`environment: production` -- GitHub la Environment protection rules set pannanum (require reviewer approval).

---

## Part 4: Pipeline Process (Visual)

```
Developer                    GitHub                         AWS
────────                    ──────                         ───
   │                           │                            │
   ├── git push (feature) ────►│                            │
   │                           │                            │
   ├── Create PR ─────────────►│                            │
   │                           ├── CI: fmt check            │
   │                           ├── CI: validate             │
   │                           ├── CI: plan ───────────────►│ (read-only)
   │                           │◄──────────────────────────┤
   │                           ├── Comment plan on PR       │
   │                           │                            │
   ├── Merge PR ──────────────►│                            │
   │                           ├── CD: apply (dev) ────────►│ (creates resources)
   │                           │                            │
   │                           ├── CD: apply (staging) ────►│ (needs approval)
   │                           │                            │
   │                           ├── CD: apply (prod) ───────►│ (needs approval)
   │                           │                            │
```

---

## Part 5: Implementation Steps

1. **Module source paths fix** -- absolute to relative (`../../modules/vpc`)
2. **Create `.gitignore`** file
3. **Create `environments/staging/`** -- copy from dev, change `tfvars` and `backend.tf` key
4. **Create `environments/prod/`** -- copy from dev, change `tfvars` and `backend.tf` key
5. **Create `.github/workflows/terraform-dev.yml`**
6. **Create `.github/workflows/terraform-staging.yml`**
7. **Create `.github/workflows/terraform-prod.yml`**
8. **Add AWS credentials** in GitHub repo Secrets
9. **Create GitHub Environments** (staging, production) with protection rules
10. **Test** -- push code, verify pipeline runs

---

## Part 6: Verification

- Push to feature branch → PR la plan output varanuma check
- Merge to main → dev apply auto run aagutha check
- Staging/Prod → manual approval prompt varutha check
- S3 la state file update aagutha check

---

## Critical Files to Create/Modify

- `environments/dev/main.tf` - Fix module source paths to relative
- `.github/workflows/terraform-dev.yml` - Dev CI/CD pipeline
- `.github/workflows/terraform-prod.yml` - Prod pipeline with approval gate
- `environments/staging/terraform.tfvars` - Staging-specific values
- `.gitignore` - Prevent state files from being committed
